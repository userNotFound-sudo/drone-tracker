node_modules/
dist/
.DS_Store
.env
.env.local
.vite/
*.log

.venv/
__pycache__/
*.pyc
.pytest_cache/

# Credentials and certificates
*.pem
*.key

# Large/binary assets — keep these out of git regardless of where they live
*.mp4
*.db
*.db-wal
*.db-shm
*.pt

# Backend data directory — JSON files may contain sensitive location config
backend/data/*.json

# Claude Code local settings (machine-specific paths)
.claude/settings.local.json
